- Додав валідацію на тіло запиту в POST {{baseURL}}/auth/signup реквесті
- додав валідацію на тіло відповіді в тому ж реквесті
- додав перевірку для оновлення користувача через PUT {{baseURL}}/users/profile . ПОМИЛКА: фото завжди повертає "default-user.png" замість того, що відправляється в баді. Окрім того, в баді ключ photo, а в респонсі photoFilename
- додав перевірку до запиту зміни паролю PUT {{baseURL}}/users/password, що userId Той самий, що й при створенні юзера
- логаут/логін працюють. В логін можна було би скопіпастити перевірку, що юзерайді той самий
- до запиту на додавання авто POST {{baseURL}}/cars/ додав перевірку, що статус дорівнює "ок"
- та сама перевірка до видалення користувача DELETE {{baseURL}}/users/
- для витрат - щоразу кількість кілометрів більша завдяки рядку у пре-реквестах let newMileage = mileage + randomMileage; так само перевірка, чи статус ок

